name = "kejmilfy"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

[vars]
# These are set via `wrangler pages secret put` or in the Cloudflare dashboard
# NEXTAUTH_SECRET, NEXT_PUBLIC_SUPABASE_URL, etc. are set as secrets/vars
